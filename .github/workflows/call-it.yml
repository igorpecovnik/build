name: Call a reusable workflow

on:

  workflow_dispatch:

jobs:

  call-workflow:
    uses: igorpecovnik/build/.github/workflows/igor.yml@master

  call-workflow-passing-data:
    uses: igorpecovnik/build/.github/workflows/igor.yml@master
    with:
      username: liza

